export interface ExistingGalleryImage {
    id: string;
    imageUrl: string;
    publicId: string;
    alt: string | null;
  }
  
  export interface ExistingCoverImage {
    url: string;
    publicId: string;
  }